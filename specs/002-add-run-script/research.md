# 調査メモ: server 配信スクリプト追加

## Decision

ローカル端末での簡易配信として、標準的な Python の HTTP 配信機能を利用する。

## Rationale

追加依存が不要で最小構成となり、ローカル用途に適合するため。

## Alternatives considered

- Node.js など別ランタイムの簡易サーバー: 追加依存が増えるため不採用
- 既存アプリケーションへの統合: スコープ外の変更になるため不採用

# GitHub Personal Access Token（fine-grained、このリポジトリのみアクセス可能）
GITHUB_TOKEN=

# Anthropic API Key（Claude Code用、`~/.claude` のログイン保存を使う場合は空でもよい）
ANTHROPIC_API_KEY=

# OpenAI API Key（Codex用、`~/.codex` のログイン保存を使う場合は空でもよい）
OPENAI_API_KEY=
